#include <iostream>

int main() {
  // && (AND)
  // || (OR)
  // ! (NOT)

  // int a = 20;
  // int b = 30;
  // std::string result = (a != b) ? "true" : "false";
  // std::cout << result;

  return 0;
}