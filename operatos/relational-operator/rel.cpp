#include <iostream>

int main() {
  // == (equal to)
  // != (not-equal to)
  // < (less than)
  // > (greater then)
  // <= (less than and equal to)
  // >= (greater than and equal to)

  // int a = 20;
  // int b = 30;
  // std::string result = (a != b) ? "true" : "false";
  // std::cout << result;

  int num1 = 5;
  int num2 = 6;
  std::string result = (num1 > num2) ? "true" : "false";
  std::cout << result;

  return 0;
}